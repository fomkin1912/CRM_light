.authform
  = form_for @activity, html: {:role => 'form'} do |f|
    %h3 New activity
    = render 'form', f: f
    = f.submit "Add activity", :class => 'button right'
    = render "shared/form_nav_links", locals: { back: true }